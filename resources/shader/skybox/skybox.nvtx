layout (location = 0) in vec3 aPos;

uniform mat4 projection;
uniform mat4 view;

out vec3 WorldPos;

void main()
{
    WorldPos = aPos;

	mat4 rotView = mat4(mat3(M_CAMERA_VIEW));
	vec4 clipPos = M_CAMERA_PROJECTION * rotView * vec4(WorldPos, 1.0);

	gl_Position = clipPos.xyww;
}