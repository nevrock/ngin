layout (location = 0) in vec3 aPos;

out vec3 WorldPos;

uniform mat4 M_PROJECTION;
uniform mat4 M_VIEW;

void main()
{
    WorldPos = aPos;  
    gl_Position =  M_PROJECTION * M_VIEW * vec4(WorldPos, 1.0);
}